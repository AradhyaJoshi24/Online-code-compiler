# Online Code Compiler

A simple Node.js + Express project that serves a browser-based JavaScript editor and runner.

## Run locally

1. Open a terminal in the project folder.
2. Run `npm install`.
3. Run `npm start`.
4. Open `http://localhost:3000` in your browser.

## Notes

- The project serves static files from `public/`.
- The editor runs JavaScript in the browser.
